{
    "name": "@generousgames/zlib-pkg-builder",
    "private": true,
    "version": "0.0.1",
    "devDependencies": {
        "@generousgames/mimi-pkg": "0.0.11",
        "dotenv-cli": "^11.0.0",
        "patch-package": "^8.0.0"
    },
    "scripts": {
        "postinstall": "patch-package",
        "build": "mimi-pkg build zlib",
        "build:dev": "npx --no-install mimi-pkg build zlib"
    }
}
